"""Core helpers for the route_opti Python playground."""

from __future__ import annotations


def greet(name: str = "traveler") -> str:
    """Return a friendly greeting for interactive demos."""
    return f"Hello, {name}! Ready to explore optimal routes?"
